/* Featured homepage bento: max projects shown per category.
   The bento layout exposes up to 8 cells per category tab (cells A–H + the
   centre text cell). Hardcoded for now; can move to SiteSettings later if the
   admin needs to tune it per category. */
export const FEATURED_MAX_PER_CATEGORY = 8;
